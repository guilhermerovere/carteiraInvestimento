import { beforeEach,describe,expect,it,vi } from "vitest";import { clearAmbiguous,freezeIntent,loadAmbiguous,RECOVERY_KEY,saveAmbiguous } from "./operation-intent";
describe("intent idempotente e recovery",()=>{beforeEach(()=>sessionStorage.clear());it("gera chave apenas ao congelar e preserva payload",()=>{vi.stubGlobal("crypto",{randomUUID:()=> "11111111-1111-4111-8111-111111111111"});const payload={valor:"0.10000001"};const intent=freezeIntent("DEPOSIT",payload);payload.valor="9.00";expect(intent.payload).toEqual({valor:"0.10000001"});expect(intent.key).toMatch(/^1111/)});it("armazena somente recovery mínimo e recupera mesma chave",()=>{const intent={version:1 as const,kind:"WITHDRAW" as const,key:"11111111-1111-4111-8111-111111111111",payload:{valor:"1.00"},createdAt:"2026-09-13T00:00:00Z",ambiguous:false};saveAmbiguous(intent);expect(loadAmbiguous()).toEqual({...intent,ambiguous:true});expect(sessionStorage.getItem(RECOVERY_KEY)).not.toContain("Authorization");clearAmbiguous();expect(loadAmbiguous()).toBeNull()})});
