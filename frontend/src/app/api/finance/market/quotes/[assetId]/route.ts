import { financeErrorResponse,financeJson } from "@/server/finance/response";import { financeBackendRequest } from "@/server/finance/transport";import { assertNoQuery,assertUuid,safeCorrelationId } from "@/server/finance/validation";
export async function GET(request:Request,{params}:{params:Promise<{assetId:string}>}){const c=safeCorrelationId(request.headers.get("x-correlation-id"));try{assertNoQuery(request);const {assetId}=await params;const r=await financeBackendRequest(`/api/v1/acoes/${assertUuid(assetId)}/cotacao`,{correlationId:c});return financeJson(r.data,r.correlationId);}catch(e){return financeErrorResponse(e,c);}}
